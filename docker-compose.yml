version: "3.8"
services:
  db:
    image: postgres:15
    container_name: inventory-db
    restart: always
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=secret
      - POSTGRES_DB=inventory
    ports:
      - "5432:5432"
